import { createDefaultRegistry } from './src/contracts/index.js'; import { demoPresentation } from './demo/public-demo.js'; import { bindKeyboard } from './src/input/keyboard.js'; import { createPlayer } from './src/player/player.js'; import { createRendererRegistry, createTextRenderers } from './src/player/renderers.js'; import { createPlayerView } from './src/ui/player-view.js';
createDefaultRegistry(); const player = createPlayer(), renderers = createRendererRegistry(); createTextRenderers().forEach((renderer) => renderers.register(renderer)); createPlayerView(document.querySelector('#app'), { player, renderers }); player.loadPresentation(demoPresentation); player.start(); bindKeyboard(document, player);
